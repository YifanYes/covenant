## Summary

<!-- 1–3 bullets describing what this PR does and why. Focus on the "why". -->

## Related Issue

<!-- e.g. Closes #123, or Relates to #456. -->

## Test Plan

- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm test:run` passes
- [ ] `pnpm build` succeeds
- [ ] Manually exercised the changed flow in `pnpm dev`
- [ ] If user-facing strings changed, i18n keys added to **both** `en` and `es` translation files

## Notes for Reviewers

<!-- Anything non-obvious: trade-offs, follow-up work, intentional scope cuts. -->
