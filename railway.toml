[build]
builder = "nixpacks"
buildCommand = "cd server && bunx prisma generate"

[deploy]
startCommand = "cd server && bunx prisma db push && bun start"
healthcheckPath = "/trpc"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
