[build]
builder = "nixpacks"
buildCommand = "pnpm install && npx prisma generate"

[deploy]
startCommand = "npx prisma db push && pnpm start"
healthcheckPath = "/trpc"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
