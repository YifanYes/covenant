meta {
  name: Update
  type: http
  seq: 3
}

post {
  url: {{host}}/habits.update
  body: json
  auth: bearer
}

headers {
  x-refresh-token: {{refresh_token}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "id": "{{habit_id}}",
    "timespan": "WEEKLY"
  }
}
