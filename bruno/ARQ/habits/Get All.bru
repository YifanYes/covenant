meta {
  name: Get All
  type: http
  seq: 2
}

get {
  url: {{host}}/habits.getAll
  body: none
  auth: bearer
}

headers {
  x-refresh-token: {{refresh_token}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "Save $100"
  }
}
