meta {
  name: Sign Up
  type: http
  seq: 1
}

post {
  url: {{host}}/auth.signUp
  body: json
  auth: none
}

body:json {
  {
    "email": "{{email}}",
    "password": "{{password}}",
    "confirmPassword": "{{password}}"
  }
}
