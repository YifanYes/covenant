meta {
  name: Get All
  type: http
  seq: 2
}

get {
  url: {{host}}/objectives.getAll
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "Save $100"
  }
}
