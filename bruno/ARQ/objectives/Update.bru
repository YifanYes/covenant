meta {
  name: Update
  type: http
  seq: 3
}

post {
  url: {{host}}/objectives.update
  body: json
  auth: bearer
}

headers {
  x-refresh-token: {{refresh_token}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "id": "{{objective_id}}",
    "name": "Train 4 times a week"
  }
}
