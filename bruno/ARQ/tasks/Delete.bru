meta {
  name: Delete
  type: http
  seq: 5
}

post {
  url: {{host}}/tasks.delete
  body: json
  auth: bearer
}

headers {
  x-refresh-token: {{refresh_token}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "id": "{{task_id}}"
  }
}

script:post-response {
  const data = res.body?.result?.data
  
  if (data) {
    bru.setEnvVar('task_id', '')
  }
}
